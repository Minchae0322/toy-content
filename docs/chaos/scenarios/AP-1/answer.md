# AP-1 — 댓글 201자: 검증 구멍 → varchar(200) 위반 → 500

> Then. Given/When은 [RUNBOOK.md](../../RUNBOOK.md) §6 AP-1. 여기엔 실행 결과를 남긴다.
> 인프라 무접촉 — 원복 없음(실패 INSERT는 롤백). **채록·채점 전에 결함을 고치지 말 것** (§6 AP 공통).

- **주입**: 250자 댓글 실요청 1건 (아무 셸, 공개 ingress)
- **hop**: 1
- **실행 일시(UTC)**: `evidence/timeline.log`

## 정답지 (원인 1줄)

댓글 DTO(@NotBlank만, @Size 부재)가 201자를 통과시켜 varchar(200) INSERT에서 DataIntegrityViolation — DB가 아니라 앱 검증 구멍이 원인(+미매핑으로 400 아닌 500).

## 근거 시그널 도달 경로

핵심: 인프라 전부 정상 + 정상 댓글은 200인데 특정 요청만 500.
Loki "Data too long" → Tempo INSERT JDBC span error → 요청 페이로드 길이로 수렴.
단발 오류라 rate 기반 메트릭에는 거의 안 보임 — **로그·트레이스 없이는 도달 불가한 문항.**

1.
2.

## 채점 앵커 — **v2 (2026-07-27 작성, 채록 전 박제 — §8.2)**

> **미실행 상태에서 박제하므로 회차 1부터 적용된다.** v2 원칙과 근거는 rca-agent
> `docs/scoring/README.md` "앵커 결함" 절.

**문항 성립 확인**: 250자 요청이 **500이면 성립**, 200이면 결함이 이미 수정된 것이므로
**불성립으로 종료**하고 그 사실을 기록한다(AP-3와 같은 취급). 채점하지 않는다.

| 항목 | 만점 | 부분점(상) | 부분점(하) | 0점 |
|---|---|---|---|---|
| 근본 원인 40 | **앱 검증 구멍(`@Size` 부재)**이 201자를 통과시켜 varchar(200) 위반 + **책임이 DB가 아니라 앱**임을 분리 | 30: 길이 제약 위반 500까지 정확하나 앱/DB 책임 분리가 모호 | 20: "DB 길이 제약 위반 500"까지 (검증 구멍 미도달) | "DB 장애" 지목 |
| 근거 경로 30 | INSERT JDBC span error + **예외 원문**(`Data too long`) + Loki 동일 로그 병기 | 20: span error와 예외 원문까지 (로그 없이 trace만으로 도달) / 또는 **동등한 대체 경로** | 10: 500 사실만 인용 | 근거 없음 |
| 오귀인 20 | DB·인프라 장애로 오진 안 함 + **DB는 제약을 정확히 지켰다**는 인식을 명시 | 10: 오진은 안 했으나 DB 책임 여부를 판단하지 않음 | — | 오진 |
| 조치 10 | `@Size(max=200)` 추가 + `DataIntegrityViolation` → 400 매핑 | 5: 둘 중 하나만 | — | 재시작 등 무관 조치 |

> **v1에서 뺀 요건**: 근거 경로의 "**정상 요청 200 공존**". v0 입력은 traceId 1개뿐이라
> **다른 요청을 볼 수단이 없다** — 에이전트가 구조적으로 달성할 수 없는 요건이다(결함 ④).
> "전면 장애가 아님"은 같은 트레이스 안에서 INSERT 앞 구간이 정상 처리된 사실로 판단하게 한다.
>
> **Loki 로그는 현행 v0로 관측 불가하다**(셀렉터 결함 5회 연속). 만점 요건에는 **남긴다** —
> trace만으로 부분점(상) 20은 도달 가능하고, 셀렉터를 고치면 30이 되어야 한다.
> **의도된 개선 측정 지점**이다(CH-1 갈래 B·AU-4와 같은 취급).

## 이 문항의 성격 — 메트릭이 원리적으로 무용하다

단발 요청 1건이라 `rate()` 기반 500 메트릭에는 사실상 안 잡힌다. 즉 **rate 알람이 못 보는
유형의 장애**이고, 그 사실 자체가 산출물이다. 에이전트가 쓸 수 있는 채널은 trace와 로그뿐이며,
현재 로그가 막혀 있으므로 **사실상 trace 단일 채널 문항**이다.

AU-2(trace 무신호·메트릭 단일 채널)와 정확히 반대 구조라 대비쌍으로 쓸 수 있다.

## 채록 전 준비

- [ ] 게이트: 정상 댓글 200 + Loki `Data too long` 최근 1h = 0건
- [ ] 250자 요청의 **traceId를 즉시 확보**해 `evidence/`에 남길 것 — `chaos.sh`가
      `tempo_search`를 호출하지 않는 문항이면 수동. 단발 요청이라 놓치면 재현이 어렵다
- [ ] 같은 창의 **정상 댓글 트레이스도 확보** (대조군). 채점자용이며 에이전트에는 주지 않는다
- [ ] 결함을 **채점 전에 고치지 말 것** (§6 AP 공통) — 고치면 문항이 소멸한다

## RCA 채점 (블라인드 — §8 루브릭, 회차는 §8.1 반복 프로토콜)

| 항목 | 배점 | 1회 | 2회 | 3회 |
|---|---|---|---|---|
| 근본 원인 정확도 | 40 | | | |
| 근거 시그널 경로 | 30 | | | |
| 오귀인 없음 | 20 | | | |
| 조치 타당성 | 10 | | | |
| **합계** | **100** | | | |

- 평균 ± 최대편차: (±10 초과 시 문항 불안정 — §8.1)
- 자기 일치도(동일 출력 2회 채점 차, ±5 초과 시 앵커 보강 — §8.2):

## evidence

- baseline: `evidence/baseline/<ts>/`
- symptom: `evidence/symptom/<ts>/`
- 채록 후 보강 커밋 (채점 전 수정 금지):
